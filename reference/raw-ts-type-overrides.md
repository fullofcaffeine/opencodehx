# Raw TypeScript Type Override Audit

Raw `@:ts.type("...")` is a last-resort interop escape hatch. Prefer ordinary
Haxe types, `DynamicAccess<T>`, narrow externs, typed decoders, or reusable
`genes.ts` helpers such as `Unknown` and `Undefinable<T>`.

Current approved overrides:

| File | Declaration | Raw type | Why it remains | Replacement direction |
| --- | --- | --- | --- | --- |
| `src/opencodehx/config/ConfigLoader.hx` | `ConfigEnv` | `{[key: string]: string \| null \| undefined}` | Process/config substitution must currently preserve three distinct host states: present string, explicit null overlay, and JavaScript undefined. | Split into a process-env facade using `genes.ts.Undefinable<String>` and a separate null-capable overlay type. |
| `src/opencodehx/resource/Resources.hx` | `ResourceUrl` | `URL` | Resource helpers pass Node URL instances, but the port does not yet have a stable cross-runtime resource/import abstraction for text/file/WASM assets. | Replace with a real URL extern or host resource facade once resource imports are generalized in `genes-ts`. |
| `src/opencodehx/externs/node/ChildProcess.hx` | `SpawnSyncStringOptions` | `import('node:child_process').SpawnSyncOptionsWithStringEncoding` | `spawnSync` overloads select return type based on encoding; this boundary keeps the overload-heavy Node type out of tool code. | Replace with a narrow structural Haxe typedef if the Node process facade needs more options. |
| `src/opencodehx/externs/node/ChildProcess.hx` | `SpawnSyncStringResult` | `import('node:child_process').SpawnSyncReturns<string>` | The host seam needs the exact string-returning spawn result without modeling the whole Node overload family. | Replace with a narrow structural Haxe typedef once all used result fields stabilize. |
| `src/opencodehx/externs/node/Fs.hx` | `NodeBufferData` | `import('node:buffer').Buffer` | `fs.readFileSync` returns Node Buffer instances. The current inline import type avoids pretending this is a Haxe-owned value. | Use the existing Node Buffer extern as the instance type and rely on `genes-ts` type-only/value import planning. |
| `src/opencodehx/externs/treesitter/WebTreeSitter.hx` | `TreeSitterTree` | `import('web-tree-sitter').Tree` | Tree-sitter owns this parser tree object; OpenCodeHX only walks it through narrow extern methods. | Replace with a generated/narrow extern class if scanner usage grows beyond current methods. |
| `src/opencodehx/externs/treesitter/WebTreeSitter.hx` | `TreeSitterNode` | `import('web-tree-sitter').Node` | Tree-sitter owns this node object; scanner code exposes typed command/path DTOs instead of leaking parser internals. | Replace with a generated/narrow extern class if scanner usage grows beyond current methods. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiFinishReason` | `import('ai').FinishReason` | AI SDK exposes a canonical package-owned finish-reason alias. The raw override keeps public generated TS aligned with the SDK even though closed OpenCodeHX-owned enum abstracts can now emit literal unions directly. | Replace with a closed Haxe enum abstract only if structural equivalence is enough and the canonical SDK alias is no longer needed at the boundary. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiSdkFactoryOptions` | `import('@ai-sdk/openai-compatible').OpenAICompatibleProviderSettings` | The OpenAI-compatible factory requires package-owned optional settings where absence is JavaScript `undefined`, not `null`. The Haxe shape uses `genes.ts.Undefinable<T>` so generated values stay null-free while the public TS type matches the SDK. | Replace with a structural/generated extern once `genes-ts` can express exact optional TS fields without adding `null`. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiBedrockFactoryOptions` | `import('@ai-sdk/amazon-bedrock').AmazonBedrockProviderSettings` | Bedrock uses a package-owned factory settings type with region, bearer `apiKey`, optional `baseURL`, headers, and an AWS credential-provider function. The Haxe shape keeps construction typed while preserving the SDK contract. | Replace with generated SDK externs if AI SDK declaration ingestion lands. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiAnthropicFactoryOptions` | `import('@ai-sdk/anthropic').AnthropicProviderSettings` | Anthropic has package-owned optional settings and currently returns `LanguageModelV2`, so the loader needs a distinct typed settings bridge rather than reusing a V3-only or OpenAI-compatible shape. | Replace with generated SDK externs if AI SDK declaration ingestion lands. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiOpenAIFactoryOptions` | `import('@ai-sdk/openai').OpenAIProviderSettings` | Official OpenAI settings add organization/project and a package-owned provider name while still requiring JavaScript `undefined` for absent fields. The Haxe backing shape preserves those options without widening to raw provider maps. | Replace with generated SDK externs if AI SDK declaration ingestion lands. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiXaiFactoryOptions` | `import('@ai-sdk/xai').XaiProviderSettings` | xAI's published settings surface omits OpenAI's `name`, organization, and project fields. A separate bridge prevents generated TypeScript from relying on extra object-literal properties. | Replace with generated SDK externs if AI SDK declaration ingestion lands. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiAzureFactoryOptions` | `import('@ai-sdk/azure').AzureOpenAIProviderSettings` | Azure OpenAI has distinct resource-name, API-version, and deployment URL settings. The bridge keeps those stable options typed while leaving token/fetch hooks for a future owned host-auth/request seam. | Replace with generated SDK externs or a typed Azure auth/fetch facade if those runtime hooks are ported. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiGoogleFactoryOptions` | `import('@ai-sdk/google').GoogleGenerativeAIProviderSettings` | Google Generative AI has package-owned settings and accepts optional header values. The Haxe shape forwards only name/baseURL/apiKey/headers while keeping fetch/generateId out until a typed request seam owns them. | Replace with generated SDK externs or a typed request facade if those runtime hooks are ported. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiVertexFactoryOptions` | `import('@ai-sdk/google-vertex').GoogleVertexProviderSettings` | Vertex settings include project/location/API-key express mode plus resolvable headers. The Haxe shape forwards stable string/header settings and deliberately omits google-auth-library option objects until a typed Google auth seam lands. | Replace with generated SDK externs or a typed Google auth facade. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiVertexAnthropicFactoryOptions` | `import('@ai-sdk/google-vertex/anthropic').GoogleVertexAnthropicProviderSettings` | Vertex Anthropic is a package subpath with no API-key field and resolvable headers. A separate bridge prevents extra properties and keeps Google auth/token hooks out of the provider map until typed. | Replace with generated SDK externs or a typed Google auth facade. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiMistralFactoryOptions` | `import('@ai-sdk/mistral').MistralProviderSettings` | Mistral's stable settings match the shared base URL, API-key, and header shape used by the simple bundled providers. Fetch and ID-generation hooks stay out until a typed host request/ID seam owns them. | Replace with generated SDK externs or a typed request/ID facade if those runtime hooks are ported. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiGroqFactoryOptions` | `import('@ai-sdk/groq').GroqProviderSettings` | Groq's stable settings match the shared base URL, API-key, and header shape used by the simple bundled providers. Keeping a package-owned raw alias preserves SDK assignability without widening provider options. | Replace with generated SDK externs or a typed request facade if those runtime hooks are ported. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiCohereFactoryOptions` | `import('@ai-sdk/cohere').CohereProviderSettings` | Cohere's stable settings match the shared base URL, API-key, and header shape used by the simple bundled providers. Fetch and ID-generation hooks stay out until a typed host request/ID seam owns them. | Replace with generated SDK externs or a typed request/ID facade if those runtime hooks are ported. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiPerplexityFactoryOptions` | `import('@ai-sdk/perplexity').PerplexityProviderSettings` | Perplexity's stable settings match the shared base URL, API-key, and header shape used by the simple bundled providers. Keeping a package-owned raw alias preserves SDK assignability without widening provider options. | Replace with generated SDK externs or a typed request facade if those runtime hooks are ported. |
| `src/opencodehx/externs/aws/AwsCredentialProviders.hx` | `AwsCredentialProvider` | `NonNullable<AmazonBedrockProviderSettings['credentialProvider']>` | The AWS credential function is created by `@aws-sdk/credential-providers` and consumed opaquely by the Bedrock SDK. OpenCodeHX should not model or inspect temporary credentials; using the exact field type avoids nullable optional credential leaks. | Replace with generated AWS/Bedrock externs if the credential-provider declaration can be imported mechanically. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiLanguageModelSpecificationVersion` | `'v3'` | `LanguageModelV3.specificationVersion` is a literal, not a broad string; the bridge lets Haxe-owned providers satisfy the SDK interface structurally. | Replace with a closed Haxe enum abstract once singleton literal emission is needed outside this SDK boundary and `genes-ts` has a focused fixture for it. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiLanguageModelSourceType` | `'url' \| 'document'` | AI SDK source content uses a literal discriminant that must not widen to plain `string` in generated result objects. | Replace with a closed Haxe enum abstract if structural SDK assignability remains identical after removing the raw alias. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiSupportedUrls` | `Record<string, RegExp[]>` | `LanguageModelV3.supportedUrls` is SDK-owned and uses JavaScript `RegExp` arrays. The Haxe adapter currently uses the synchronous record arm. | Replace with a generated/structural SDK extern if supported URL maps become a broader app-facing abstraction. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiProviderOptions` | `import('@ai-sdk/provider').SharedV3ProviderOptions` | Provider options are SDK JSON object records; Haxe reads only the OpenAI-compatible subset needed by Copilot. | Replace with generated SDK externs or typed decoders for provider-specific option schemas. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiJsonObject` / `AiJsonValue` / `AiNonNullJsonValue` | `import('@ai-sdk/provider').JSONObject` / `JSONValue` / `NonNullable<JSONValue>` | SDK provider metadata and tool results distinguish JSON objects, JSON values, and non-null JSON result payloads from arbitrary unknown values. These bridges keep metadata/results strict while using `Unknown` at runtime boundaries. | Replace with shared JSON value/object types if a generic decoder/value model lands. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiProviderMetadata` | `import('@ai-sdk/provider').SharedV3ProviderMetadata` | SDK result metadata is a package-owned `Record<string, JSONObject>`; Copilot maps typed prediction/reasoning metadata into that exact surface. | Replace with generated SDK externs once metadata maps are modeled structurally. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiLanguageModelPrompt` | `import('@ai-sdk/provider').LanguageModelV3Prompt` | Copilot accepts the SDK prompt union at the boundary, then narrows it into Haxe prompt enums before request conversion. | Replace with generated discriminated-union support when `genes-ts` can emit/read this SDK union natively. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiLanguageModelResponseFormat` / `AiLanguageModelTool` / `AiLanguageModelToolChoice` | `NonNullable<LanguageModelV3CallOptions[...] >` | These are SDK discriminated unions. Raw aliases keep generated option objects assignable while Haxe backing shapes expose the fields the adapter consumes. | Replace with generated SDK externs or Haxe enum-to-TS-union emission for these unions. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiLanguageModelCallOptions` | `import('@ai-sdk/provider').LanguageModelV3CallOptions` | The exact provider call surface is SDK-owned; the Copilot adapter narrows it into Haxe DTOs without production casts. | Replace with generated SDK externs if the AI SDK declarations are imported mechanically. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiLanguageModelContent` / `AiLanguageModelWarning` / `AiLanguageModelV3Usage` / `AiLanguageModelFinishReason` | AI SDK `LanguageModelV3*` result aliases | Generated/provider results must be structurally accepted by AI SDK consumers while Haxe tests can still inspect fields through backing shapes. | Replace with generated SDK externs or first-class TS discriminated-union support in `genes-ts`. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiLanguageModelGenerateResult` / `AiLanguageModelStreamResult` | AI SDK `LanguageModelV3GenerateResult` / `LanguageModelV3StreamResult` | Copilot returns exact SDK result surfaces from a Haxe-owned implementation; raw aliases avoid local lookalike result types leaking into TS. | Replace with generated SDK externs once available. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiLanguageModel` / `AiLanguageModelV3` | `import('@ai-sdk/provider').LanguageModelV3 \| import('@ai-sdk/provider').LanguageModelV2` / `import('@ai-sdk/provider').LanguageModelV3` | Real bundled SDKs do not all return V3 models; current Anthropic returns `LanguageModelV2`, while Cloudflare Gateway explicitly requires V3. The pair keeps the general loader honest and preserves V3-only assignability where needed. | Replace with generated provider externs if AI SDK declaration ingestion lands. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiTool` | `import('ai').Tool` | AI SDK tools are heavily generic and inferred by `tool(...)`; Haxe constructs them through typed `AiToolOptions<I, O>` only at the extern boundary. | Replace with a structured helper once `genes-ts` has a generic TS type IR for SDK generic aliases. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiJsonSchema` | `import('ai').JSONSchema7` | AI SDK's schema adapter expects its package-owned JSON Schema alias. | Replace with a shared JSON Schema extern/facade if schema use expands beyond AI SDK tools. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiProviderStreamPart` | `import('@ai-sdk/provider').LanguageModelV3StreamPart` | `ai/test` and the Copilot adapter consume the SDK's stream-part union. The Haxe backing shape now lets constructors and stream mapping emit without casts. | Replace with generated union emission from Haxe enums/typedefs when `genes-ts` can print this SDK union directly. |
| `src/opencodehx/externs/ai/AiSdk.hx` | `AiProviderReadableStream` | `ReadableStream<import('@ai-sdk/provider').LanguageModelV3StreamPart>` | The AI SDK requires a `ReadableStream<LanguageModelV3StreamPart>`. The Haxe abstract now wraps the local generic Web stream extern. | Remove once the standard or project Web stream extern can carry this exact generic type without a raw alias. |
| `src/opencodehx/externs/hono/NodeServer.hx` | `NodeServerType` | `import('@hono/node-server').ServerType & { closeAllConnections?: () => void; closeIdleConnections?: () => void }` | `@hono/node-ws` expects Hono's canonical package-owned server type, while Node HTTP close helpers are only present on some server variants. The Haxe abstract now has a structural underlying shape for the lifecycle methods OpenCodeHX calls, so route code no longer needs a raw `Syntax.code` lifecycle block. | Keep the structural method subset narrow; remove the raw override only if `genes-ts` can express package-owned return types and structural Haxe method access without losing TypeScript assignability. |
| `src/opencodehx/externs/hono/Hono.hx` | `HonoHandler` | `import('hono').Handler<any, string, any, any> \| import('hono').MiddlewareHandler<any, string, any, any>` | Hono's handler/middleware overload surface is library-specific and currently needed for route and websocket adapter assignment. | Hide behind a route adapter and narrow `HonoContext`/handler facades; keep `genes-ts` free of Hono-specific knowledge. |
