# OpenCodeHX Docs

Design notes, parity matrices, host-seam ledgers, guardrails, and decision records live here.

Prefer checked-in evidence over transient notes. If a lesson changes how agents should work in this repo, update `AGENTS.md` in the same change.

Current executable evidence includes the credential-free fake provider transcript harness in `fake-provider-transcript-harness.md`, the typed provider registry, first AI SDK stream/OpenAI-compatible/Bedrock factory smokes, first provider transform fixtures, and models.dev cache/fetch smoke in `provider-registry-port.md`, the resource import adapter with generated manifest and worker packaging fixtures in `resource-imports.md`, the compiler output profile decision in `compiler-output-profiles.md`, the shared error diagnostics golden in `error-diagnostics-parity.md`, the headless run scaffold including default and OPENCODE_DB-backed run persistence/export/resume append/continue/fork for deterministic, mock AI SDK, and local live AI SDK runs, `run --mock-ai-sdk` with recovered text-history prompt evidence, `run --dir`, `run --file` local file/directory attachment parts, `run --session`, `run --continue`, live `run --model provider/model` routing plus plain config-model `run` with global/project config, auth storage, well-known remote config discovery, active-account remote config loading, local no-network OpenAI-compatible streaming success, live `read`/`write`/`edit`/`apply_patch`/`bash` tool-call continuation with write/edit/patch/bash side-effect verification, and provider-error transcript/export evidence, plus non-interactive `export <sessionID>` in `headless-run-scaffold.md`, the upstream CLI command-surface catalog and alias/help/export/resume/fork smoke in `cli-command-surface.md`, the local npm global packaging path including installed `run --dir`, installed `run --file`, installed `run --mock-ai-sdk --dir`, installed live `run --model provider/model` plus plain config-model `run` against local OpenAI-compatible success, read/write/edit/apply_patch/bash tool-call, and provider-error endpoints, installed default and OPENCODE_DB run/export/resume append/continue/fork including live runs, installed TUI scaffold execution through package-local Bun, and installed `serve` health/SSE/session/PTY workflow in `npm-global-packaging.md`, the Bun binary packaging feasibility report in `bun-binary-packaging-feasibility.md`, the callback and stream-shaped bus runtime in `bus-runtime-parity.md`, the Node-backed shell seam with Windows shell CI harness in `bash-shell-seam.md`, the PTY runtime lifecycle and WebSocket replay seam in `pty-runtime.md`, the permission model in `permission-model-port.md`, the project/git/worktree/instance-bootstrap/sync runtime foundation, opt-in native file-watcher harness, and opt-in live package-manager harness in `project-runtime-parity.md`, the formatter service seam in `formatter-port.md`, the pure observability resource parser, run-service memo evidence, and instance-state lifecycle evidence in `effect-runtime-parity.md`, the smoke tmpdir fixture parity in `fixture-smoke-parity.md`, the generated TypeScript readability gate in `generated-ts-readability-gate.md`, the one-turn plus async AI SDK-backed session processor and store-backed export fixture in `session-processor-one-turn.md`, the first Node/Hono server seam and SDK-compatible create/list/resume/event smoke in `server-hono-seam.md`, the first MCP/ACP minimum protocol surface in `mcp-acp-minimum.md`, the first LSP runtime/client/tool seam in `lsp-minimum.md`, the first plugin runtime metadata/loader/trigger seam in `plugin-runtime-minimum.md`, the repo-wide raw `Syntax.code` classification in `syntax-code-audit.md`, source-authored checked artifact constructors in `checked-artifact-constructors.md`, source-authored tool-ID macro diagnostics in `tool-registry-port.md`, the first performance/UX benchmark harness in `performance-ux-benchmark.md`, the source-level portability classification ledger in `portability-classification-ledger.md`, and the TUI scaffold/transcript replay path plus route/keybind macro diagnostics in `tui-scaffold.md`.

The top-level `README.md` carries the public-facing ASCII progress bar. Refresh it from Beads when closing meaningful port slices so newcomers see a current completion snapshot without digging through issue history.

Public-readiness checks live in `scripts/ci/version-sync-check.mjs`, `scripts/ci/release-contracts-check.mjs`, `.github/workflows/ci.yml`, `.github/workflows/release.yml`, and `.github/workflows/security-gitleaks.yml`. The release setup intentionally stays on `0.x` beta prereleases until OpenCode parity and packaging smoke evidence justify stable release claims.

Testing strategy:

- `haxe-authored-testing-strategy.md` records the plan for Haxe-authored test facades that generate idiomatic Jest/Vitest and Playwright specs while preserving upstream OpenCode tests as oracles.

Planning matrices:

- `opencode-source-inventory.md` summarizes the upstream source inventory and points to `reference/opencode-source-parity-matrix.csv`.
- `opencode-test-port-matrix.md` summarizes per-test port status and points to `reference/opencode-test-port-matrix.csv`.
- `upstream-rebase-procedure.md` defines the controlled process for refreshing the `../opencode` oracle snapshot, reviewing matrix drift, updating evidence, and creating Beads for unresolved upstream changes.
- Regenerate source/test matrices with `npm run inventory:matrix`.

Decision records:

- `m3-replacement-checkpoint.md` records the 2026-06-18 M3 decision to continue the port, keep the next phase Node-first/server-focused, and defer replacement claims until broader parity evidence exists.
